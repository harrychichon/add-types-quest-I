export type Dog = {
  name: string;
  color: string;
};

export type User = {
  name: string;
  hobby: string[];
  age: number;
   
};

export type Monster = {
  name: string;
  age: number;
  tentacles: number;
  eyes: number;
  hasWings: boolean;
};